<h1 class="text-center mt-4">Categoria: <%= @category.name %></h1>
<% if logged_in? && current_user.admin? %>
    <%= link_to 'Editar', edit_category_path(@category.id), class: "btn btn-outline-info" %>
<% end %>

<%= render 'shared/pagination', obj: @category_articles %>

<%= render 'articles/articles', obj: @category_articles %>

<%= render 'shared/pagination', obj: @category_articles %>

